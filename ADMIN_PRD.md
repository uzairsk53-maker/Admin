You are a Senior Angular UI/UX Engineer.

TASK:
Upgrade ONLY the UI of an existing Admin Panel into a PREMIUM, MODERN, MNC-LEVEL DASHBOARD (Stripe/Shopify style).

IMPORTANT:
- DO NOT change backend APIs
- DO NOT change business logic
- ONLY improve UI/UX and components styling
- NO dummy or static UI
- EVERYTHING must remain production connected

---

# ❌ STRICT RULES
- DO NOT use Angular Material (uninstall if present)
- DO NOT use default browser alerts
- DO NOT use basic HTML forms or ugly inputs
- DO NOT use old UI patterns

---

# 🎨 UI REQUIREMENTS (VERY IMPORTANT)

## Design Style:
- Premium modern UI (Stripe / Shopify / Notion admin style)
- Glassmorphism + Soft UI blend
- Clean spacing system (8px grid)
- Rounded corners (12px–20px)
- Subtle gradients + soft shadows

---

## 🧩 FORMS (UI ONLY IMPROVEMENT)

Replace all forms with:

- Floating label inputs
- Animated focus state (glow border)
- Inline validation under fields (NO alerts)
- Card-based form layout
- Grouped sections for better UX

---

## 📊 TABLES

Replace all tables with:

- Custom Angular tables (NO Material table)
- Sticky header
- Row hover animation
- Smooth pagination UI
- Search bar with debounce UI effect
- Clean column spacing

---

## 🪟 MODALS

- Center animated modal
- Blur background effect
- Smooth scale + fade animation
- No abrupt open/close

---

## 🔔 TOAST / NOTIFICATIONS

- Custom toast system (top-right)
- Types:
  - success (green glow)
  - error (red glow)
  - warning (yellow glow)
  - info (blue glow)
- Auto dismiss with smooth animation

---

## 🧭 NAVIGATION

- Sidebar redesign (premium UI)
- Smooth collapse/expand animation
- Active route glow effect
- Icon + label clean layout
- Header with:
  - search bar
  - notifications
  - profile dropdown

---

# ⚡ ANIMATIONS (MANDATORY)

Every UI element must have micro-interactions:

- Button hover (scale + glow)
- Card hover (lift effect)
- Page transitions (fade + slide)
- Input focus (animated border glow)
- Sidebar transitions (smooth expand/collapse)
- Modal open/close animation

---

# 📱 RESPONSIVENESS

- Fully mobile responsive
- Sidebar becomes bottom navigation on mobile
- Tables convert into card view on small screens
- Forms stack vertically on mobile

---

# ⚙️ PERFORMANCE (UI SIDE ONLY)

- Debounced search UI behavior
- Smooth rendering (no flicker)
- Lightweight DOM updates
- No unnecessary re-renders 

---

# 📦 DEPENDENCY RULE

- Remove Angular Material completely if exists
- Use ONLY custom UI components or lightweight libraries (if needed)
- If installing any UI library, explain why before using it

---

# 🏁 OUTPUT REQUIRED

- Updated Angular UI components only
- Improved folder structure for UI components
- Fully responsive premium UI
- Animation system applied everywhere
- No business logic change
